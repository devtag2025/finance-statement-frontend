/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'via.placeholder.com',
      'ai-story-telling-bucket-v1.s3.eu-west-2.amazonaws.com'
    ],
  },
};
export default nextConfig;
